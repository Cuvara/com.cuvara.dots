namespace Cuvara.DOTS
{
    /// <summary>
    /// Placeholder type so the <c>Cuvara.DOTS.Runtime</c> assembly is produced by Unity.
    /// An assembly definition whose folder contains no C# file yields no assembly, and every
    /// assembly referencing it then fails to resolve. Delete this file once real runtime code lands.
    /// </summary>
    public static class PackageMarker
    {
        /// <summary>Package name as declared in <c>package.json</c>.</summary>
        public const string PackageName = "com.cuvara.dots";
    }
}
